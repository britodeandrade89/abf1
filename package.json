{
  "name": "abfit-pwa",
  "private": true,
  "version": "1.1.0",
  "description": "Assessoria de Treinamento Físico por André Brito.",
  "scripts": {
    "build": "rimraf public && esbuild index.tsx --bundle --minify --outfile=public/index.js --external:@google/genai --external:@capacitor/cli && cpx \"./*.{html,css,json}\" public && cpx \"service-worker.js\" public"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.12",
    "@types/marked": "^6.0.0",
    "cpx": "^1.5.0",
    "esbuild": "^0.20.2",
    "rimraf": "^5.0.5"
  }
}